# stellaryard-cli
stellaryard-cli is a terminal client for StellarYard. It talks to stellaryard-core's REST/WS API — same contract the dashboard consumes — but exposes it as scriptable commands instead of a browser UI.
